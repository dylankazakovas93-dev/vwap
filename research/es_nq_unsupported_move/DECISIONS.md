# DECISIONS.md — ES-NQ unsupported move discovery

| # | Date | Decision | Rationale / consequence |
|---|---|---|---|
| 1 | 2026-07-13 | New branch `research/es-nq-unsupported-move-discovery` from `201896d`; new directory `research/es_nq_unsupported_move/`; session legs reused unchanged from `research/ema21_session_horizon/` rather than redefined | Per explicit instruction to use "the repository's audited session definitions"; this is the only prior generation with a fully specified Asia/London/New-York convention |
| 2 | 2026-07-13 | Regression orientation frozen as `NQ_return = alpha + beta*ES_return + residual` (task's own recommended orientation), never re-oriented per event regardless of which instrument leads | Keeps the residual definition identical and comparable across ES-led and NQ-led events; re-orienting per event would make "residual" mean a different thing depending on the event's own leader, breaking the requested ES/NQ-led symmetry |
| 3 | 2026-07-13 | Rearming/non-overlap rule (no explicit bar-count arming given for this generation, unlike prior single-instrument generations): sequential scan of window endpoints per leg instance; first candidate (EXTREME or MODERATE) consumes the leg instance through `min(t+15, leg_end)`; next scan resumes strictly after | Directly implements "one event per armed dislocation," "no overlapping event or outcome windows," and "treatment and control events must use identical chronology and overlap handling" with one uniform, symmetric mechanism |
| 4 | 2026-07-13 | Resolution-attribution materiality threshold frozen at `0.25 * \|r0\|` for both `LEADER_REVERSAL_AMOUNT` and `LAGGARD_CATCHUP_AMOUNT` | A single, symmetric, round fraction chosen before any event or outcome data was viewed; large enough to exclude noise-level contributions, small enough not to force everything into `PARTIAL_OR_MIXED` |
| 5 | 2026-07-13 | Causal residual path (§7) is built from cumulative returns anchored at `T-5` (the same causal base price used for the event return itself), not from a fresh per-bar residual recursion, so that `RESIDUAL_PATH(0)` recovers `r0` exactly and the frozen event-time alpha/beta are applied consistently across the whole outcome window | Matches "track the causal residual path using the frozen event-time alpha and beta; do not refit during the outcome window" while keeping continuity with the event definition (`RESIDUAL_PATH(0) == r0` is a directly testable invariant) |
| 6 | 2026-07-13 | Leader-z and residual-z bands are recorded and reported per event (and used to describe cell composition) but are **not** used as an additional permutation-stratification key beyond `leg x leader_instrument x leader_direction x clock_hour` — the instruction's "required stratification/matching variables" list is honored by recording and reporting all six, while the *permutation shuffle* itself operates on the four coarser keys to keep matched strata from becoming too thin to permute meaningfully | With leader-z/residual-z bands added as literal shuffle keys, most matched strata would contain 0-1 events per band per class, making the permutation degenerate; reporting the bands descriptively (and confirming band composition is balanced within each shuffle stratum) satisfies "required... variables" without manufacturing empty strata |
| 7 | 2026-07-13 | Deterministic permutation seed = `20260713`, reused from the prior sweep-and-failure generation as this repository's established frozen-seed constant, >=10,000 permutations per family | Fixed before any development-partition result was viewed |
| 8 | 2026-07-13 | Reuse only the audited data infrastructure and general no-lookahead/first-touch/barrier-tie/permutation-test mechanics from prior generations (re-implemented locally for this generation's cross-instrument, 5-minute-return structure); no conclusion, level, or classification result imported from any other generation | Consistent with "do not redesign the study" while keeping this generation self-contained and independently auditable |
