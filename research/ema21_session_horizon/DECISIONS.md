# DECISIONS.md — EMA21 session-and-horizon decomposition

| # | Date | Decision | Rationale / consequence |
|---|---|---|---|
| 1 | 2026-07-13 | New branch `research/ema21-session-horizon-surface` from `7853a9c`; new directory `research/ema21_session_horizon/`; only `FULL_SESSION_EMA21` computed, no EMA20/22, no other indicator | Per explicit instruction: decompose the same question by session/timing, do not redesign or add indicators |
| 2 | 2026-07-13 | Development partition guard identical in mechanism to the base generation (`session_date` filter applied immediately after loading the parquet); ES/NQ fully separate end-to-end | Consistency with the audited pattern; testable the same way |
| 3 | 2026-07-13 | Asia's midnight wrap is handled via the existing root `session_date = date(ET+6h)` convention rather than a bespoke wrap calculation: bars 18:00-23:59 (prev. calendar date) and 00:00-02:59 (session_date's own calendar date) already share one `session_date`, so grouping by `(session_date, "ASIA")` and sorting by `ts_event` yields one correctly ordered leg | Verified by construction and by test #9 against a synthetic bar set spanning a real midnight boundary |
| 4 | 2026-07-13 | Event-arming state resets at the start of every `(session_date, leg)` instance; arming, touch, and every post-touch outcome/barrier window are computed on that leg instance's own local bar array, never the global continuous array | Matches "an event may not arm in one session and touch in another" and "no outcome crossing session boundaries" exactly, and makes both mechanical rather than heuristic |
| 5 | 2026-07-13 | ATR20/TR remain computed on the full continuous bar sequence (not reset per leg), same as the EMA itself — only arming/touch/outcome windows are leg-scoped | Spec explicitly reuses "the same causal five-minute ATR20 definition as the prior generation," which was never session-scoped; ATR is a volatility-scale input, not part of the event/outcome state machine being reset |
| 6 | 2026-07-13 | Touch-time bucket computed from 1-indexed local bar rank within the leg instance (chronological `ts_event` order), not from clock time-of-day | Matches "bars 1-6 / 7-12 / 13-24 / 25+" wording exactly and is wrap-safe by construction (Asia's local rank 1 is always its true first bar regardless of calendar-date crossing) |
| 7 | 2026-07-13 | Primary BH family = `instrument x session`, membership = all `touch_time_bucket (4, disjoint) x approach_side (2) x horizon (9)` = 72 members; `ALL_SESSION` touch-time-bucket cells are a separate exploratory family, never pooled into the primary family | `ALL_SESSION` overlaps/duplicates the four disjoint buckets (it is their union), so including both would double-count events under one BH pass and violate the disjointness assumption implicit in "reject/accept a hypothesis" family framing |
| 8 | 2026-07-13 | Session-level mechanism classification requires directional agreement across >=2 adjacent horizons AND >=2 adjacent touch-time buckets (adjacency defined on the natural ordered lists), with a single exception waiving the adjacent-bucket requirement when the finding is confined entirely to `SESSION_0_TO_30`, plus >=4/5-year sign agreement pooled across the corroborating cells | Directly implements "do not promote one isolated significant cell" and the explicit first-30-minutes exception |
| 9 | 2026-07-13 | Reuse the base generation's audited data infrastructure and 3-bar arming / mirrored-outcome / barrier-tie mechanics unchanged in form (only re-scoped to per-leg local arrays); reuse is of infrastructure/mechanics, not of any conclusion, level, or classification result | Consistent with "do not redesign the hypothesis" — the arming/outcome/barrier machinery is the shared no-lookahead engineering discipline the task explicitly allows carrying forward |
