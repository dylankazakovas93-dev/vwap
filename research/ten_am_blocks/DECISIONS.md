# DECISIONS.md — 10:00 open / pre-10:00 rejection-block discovery

| # | Date | Decision | Rationale / consequence |
|---|---|---|---|
| 1 | 2026-07-13 | New branch `research/ten-am-rejection-block-discovery` from `201896d`; new directory `research/ten_am_blocks/`; works directly on 1-minute bars (no 5-minute aggregation); no ICT narrative claims anywhere, blocks defined as pure OHLCV geometry | Per explicit instruction: OHLCV geometry only, independent generation |
| 2 | 2026-07-13 | Development partition guard identical in mechanism to prior generations (`session_date` filter applied immediately after loading the parquet); ES/NQ fully separate end-to-end | Consistency with the audited pattern; testable the same way |
| 3 | 2026-07-13 | `MEDIAN_RANGE_20` counts the 20 chronologically prior *valid* sessions (RANGE_30>0, all 30 pre-10:00 bars present), not the 20 prior calendar sessions — an invalid session is simply skipped when accumulating the trailing window | The spec requires "all 20 prior observations" of a *causal scale value*, which does not exist for an invalid session; counting calendar sessions would silently inject a missing/undefined value into the median |
| 4 | 2026-07-13 | "Last bar whose high equals the final 30-minute high" (and mirrored low rule) resolved by scanning bars 0..29 in chronological order and keeping the most recent match; when the extreme is set by exactly one bar this is unambiguous, when several bars tie at the extreme the most recent one is chosen, exactly as the word "last" specifies | Direct reading of "the last bar ... whose high equals the final 30-minute high"; verified against a synthetic tie fixture (test #8) |
| 5 | 2026-07-13 | Block interaction = ANY overlap between an interaction-window bar's `[Low,High]` range and the zone `[BLOCK_LOW,BLOCK_HIGH]` (not full containment, not a single boundary touch) | "A bar's range overlaps any part of the zone" is the literal instruction; a bar that only clips the edge of the zone still counts |
| 6 | 2026-07-13 | Nested activation windows are derived from the single stored first-interaction bar `T` by comparing `T - 600` (minutes into the post-10:00 window) against 0/5/10/15/30/60, never re-searched independently per window | Matches "derive all activation windows from the one first-interaction timestamp. Do not search independently for each window" exactly |
| 7 | 2026-07-13 | Module B's post-interaction *outcome* horizons (1-60 min) are allowed to read 1-minute bars past 10:59 ET (up to 11:59 for the 60-minute horizon from a late interaction); only the *interaction search itself* is capped at 10:59 | The spec caps "post-10:00 interaction search" at 10:59 but defines outcome horizons independently in minutes from `T`, with "no partial-window fallback" as the only completeness constraint — capping outcomes at 10:59 as well is not stated and would silently truncate every interaction found late in the search window |
| 8 | 2026-07-13 | Module A's 15-minute-horizon barrier/excursion outcomes may also read past 10:59 (10:01+15=10:16, always inside 10:59, so this is moot for the primary test, but noted for the 30/60-minute exploratory Module-A horizons) | Same reasoning as #7, applied consistently to Module A |
| 9 | 2026-07-13 | `NEUTRAL_1000` sessions are retained in the Module-A ledger and counted, but excluded from the `TEN_AM_CONTINUATION`/`TEN_AM_REVERSAL` directional summary cells (continuation/reversal direction is undefined without a bullish/bearish state) | Continuation/reversal are only defined relative to a directional 10:00 candle; a neutral candle has no continuation or reversal side by construction |
| 10 | 2026-07-13 | Module A's strong, ubiquitous "continuation" finding is reported together with an explicit structural caveat: barriers/outcomes are anchored to `O_1000` while the candle *state* is defined by `Close_1000`, so the post-`T+1` price path necessarily starts already displaced toward the continuation side by the candle's own body -- this is not corrected or redesigned (the anchoring is exactly as specified in `SPEC_TEN_AM_BLOCKS.md`, mirroring the task's own Module A definition), only disclosed prominently in `TEN_AM_REJECTION_BLOCK_REPORT.md` and here | Per the charter's commitment to not overclaim a mechanical artifact as a discovery (mirrors the base generations' treatment of the motivating screenshot); "do not redesign the study" forbids re-anchoring the comparison to `Close_1000` even though that would be the natural bias-free alternative |
| 11 | 2026-07-13 | Reuse only the audited data infrastructure and general no-lookahead/first-touch/barrier-tie mechanics from prior generations (same *form*, re-implemented locally for this generation's 1-minute, 09:30-10:59-anchored structure); no conclusion, level, or classification result imported | Consistent with "do not redesign the study" while keeping each generation's engine self-contained and independently auditable |
