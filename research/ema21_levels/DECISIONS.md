# DECISIONS.md — intraday EMA21 level discovery

| # | Date | Decision | Rationale / consequence |
|---|---|---|---|
| 1 | 2026-07-12 | New branch `research/intraday-ema21-level-discovery` from `201896d`; new directory `research/ema21_levels/`; no prior generation's levels/taxonomy/methodology imported, only the audited 1-minute data infra and no-lookahead discipline | Per explicit instruction: independent generation |
| 2 | 2026-07-12 | Development partition enforced by one explicit filter (`session_date` in `[2018-01-03, 2022-12-30]`) applied immediately after loading the processed parquet, before any 5-minute bar/EMA/event code runs; no function in `src/` accepts or reads a later date | Hard 2023+ access guard, testable by static scan of emitted ledgers |
| 3 | 2026-07-12 | 5-minute bars are dropped (not partially aggregated) unless all 5 constituent 1-minute bars are present | Matches "no partial five-minute bars" instruction exactly |
| 4 | 2026-07-12 | EMA warm-up = 60 completed 5-minute bars minimum before any bar is eligible to arm/touch (~2.7 RTH sessions of RTH bars, or ~5 hours of full-session bars) | Conservative multiple of the largest span (22) so `ewm(adjust=False)` transients are negligible before any event is counted; chosen before any event data was viewed |
| 5 | 2026-07-12 | Arming is a rolling 3-bar check re-evaluated every bar (not a one-shot latch); a touch immediately disarms and a fresh 3-bar one-sided run is required before the next event | Prevents oscillation from creating repeated pseudo-events, per instruction; makes "at most one event per armed excursion" mechanical rather than heuristic |
| 6 | 2026-07-12 | `FULL_SESSION_EMA`'s 3-bar arming bars may include overnight (non-RTH) bars, since the EMA/bar sequence for that definition is the continuous full-session sequence; only the touch bar `T` itself is restricted to RTH | Matches instruction "evaluate touches only during RTH" while keeping the full-session EMA's own bar sequence genuinely continuous |
| 7 | 2026-07-12 | Barrier-first search window for `(horizon H, barrier b)` is `T+1 .. T+H`, i.e. barrier tests are horizon-scoped, not evaluated once over the full 20-bar ledger and then reused | Keeps "barrier-first within horizon H" well-defined and consistent with the excursion outcomes computed over the same window |
| 8 | 2026-07-12 | Reuse generation-1's audited `data/processed/{es,nq}_front_1m.parquet` unmodified; re-ran `data_build.py` unchanged and re-verified counters/hashes rather than trusting a cached copy | Confirms the inherited data infra is bit-identical to the audited root contract before building on it |
| 9 | 2026-07-12 | No SMA/VWAP/MACD/RSI, no other EMA period, no regime/weekday/month/economic-release filters, no entries/exits/sizing/TP-SL anywhere in this generation | Per explicit prohibited-analyses list; a null EMA21 result is reported as null, not rescued by a new filter |
