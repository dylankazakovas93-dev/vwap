# DECISIONS.md — generation 9 (simple cash-open level study)

Implementation-level judgment calls required to execute Dylan's frozen
methodology exactly; none alter its substance, thresholds, or scope.

| # | Decision | Rationale |
|---|---|---|
| 1 | New branch `research/simple-cash-open-level-study` from `research/cash-open-target-atlas` @ `201896d`; does not import generations 6-8's code | Per instruction: independent generation, no inheritance of the 38-level library/synthetic-control/taxonomy |
| 2 | Self-contained dense-bar-array builder (with volume) written locally, not importing generation 3's `atlas.py` (`build_dense_bars`, `tau_max=59` ceiling) | This generation needs bars through `et_minute=809` (touch as late as 689 + 120-min horizon), far beyond atlas.py's 59-bar ceiling; atlas.py also lacks a volume field. Writing a local variant (same `et_minute`/`tau` convention) avoids modifying a prior generation's file |
| 3 | Outcome/barrier storage is long-format (one row per event×horizon, one row per event×horizon×b), not wide-column-per-horizon | Same rationale as generation 8: tractable at 86 levels × 2 arms(N/A here, single real level)× up to ~1290 sessions scale; frozen spec does not mandate a storage shape |
| 4 | `native_center`/`native_sd` for Family A = `VWAP_ON`/`SD_ON` or `VWAP_PR`/`SD_PR` (the anchor's own center/SD); for Family B = the side-appropriate `CENTER_{U,D}(N,CENTER)`/`SD_{U,D}(N)` used to build that specific level | "native_sd" is the frozen spec's own normalization basis (§13, §14); this pins exactly which SD each level_id uses without introducing a new, unrelated scale |
| 5 | Same-bar `CLOSE==level_value` neutral case tested with exact floating-point equality on the raw price series (no tolerance band) | The frozen spec's §10 formulas use exact `==`; introducing a tolerance would be an unrequested design change. In practice, exact equality is rare but not impossible at whole/half-tick prices |
| 6 | Alias/coincident-level detection (§19) uses the instrument tick size (ES/NQ both 0.25) as the "within one tick" threshold, and exact floating-point equality (post round-to-tick) for "exact duplicates" | Matches the frozen spec's own tick-size framing directly |
| 7 | Behavior classification's "exact binomial test survives the stated multiplicity correction" (§17) is read as: the BH-adjusted q-value for that specific `(instrument, level_id, primary horizon)` cell's 1.0-SD barrier test is `< 0.05` | This is the only multiplicity correction defined for the 1.0-SD barrier test (§18); no separate correction is introduced for the classification step itself |
| 8 | Year-stability "4-of-5-year sign stability" is evaluated on the **raw sign** of `(continuation_first_rate − reversal_first_rate)` per year at the 1.0-SD barrier, restricted to years meeting the ≥10-touch floor; if fewer than 4 years meet the floor, the level cannot satisfy this condition (falls to `MIXED_OR_NULL` or `UNDERPOWERED`) | Directly operationalizes §17's condition 4 using the year-stability data already required by §16 |
| 9 | `DOMINANCE_h` and normalized outcomes use `native_sd` even when the level's own `k=0`/rung has `SD=0` in a degenerate synthetic edge case; per §13, `native_sd==0` excludes normalized outcomes for that level_id entirely (all sessions), not just the affected session | Matches the frozen spec's literal instruction ("If native_sd==0, retain raw outcomes but exclude normalized outcomes") applied at the level_id's defining SD, not per-session |
